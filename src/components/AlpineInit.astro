---

---

<script>
  import Alpine from "alpinejs";
  import intersect from "@alpinejs/intersect";

  // Register the intersect plugin
  Alpine.plugin(intersect);

  // Start Alpine (this is already being done, but included for completeness)
  window.Alpine = Alpine;
  Alpine.start();
</script>
