---
// This component only handles Alpine reinit during view transitions
---

<script>
  document.addEventListener("astro:page-load", () => {
    // Only reinitialize components after view transitions
    if (window.Alpine) {
      window.Alpine.initTree(document.body);
    }
  });
</script>
