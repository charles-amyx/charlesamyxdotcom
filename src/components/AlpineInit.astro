---

---

<script>
  import Alpine from "alpinejs";
  import intersect from "@alpinejs/intersect";

  // Register the intersect plugin
  Alpine.plugin(intersect);

  // Start Alpine (with type assertion)
  (window as any).Alpine = Alpine;
  Alpine.start();
</script>
